# Generated by Vite — see web/vite.config.ts build.outDir.
# placeholder.html is the committed stand-in served when the SPA hasn't
# been built (fresh checkout, CI before `make web`). Vite's emptyOutDir
# is off so this .gitignore survives rebuilds; the Makefile's `web`
# target clears the generated artefacts below before invoking Vite.
index.html
assets/
favicon.ico
slipspace.png
slipspace.svg
