# Generated by Vite — see web/vite.cp.config.ts build.outDir.
# placeholder.html is the committed stand-in served when the SPA hasn't
# been built (fresh checkout, CI before `make web-cp`). Vite's emptyOutDir
# is off so this .gitignore survives rebuilds; the Makefile's `web-cp`
# target clears the generated artefacts below before invoking Vite.
index.html
index.cp.html
assets/
favicon.ico
sluice.png
sluice.svg
