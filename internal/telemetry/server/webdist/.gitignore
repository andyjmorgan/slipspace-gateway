# Generated by Vite — see web/vite.telemetry.config.ts build.outDir.
# placeholder.html is the committed stand-in served when the SPA hasn't been
# built (fresh checkout, CI before `make web-telemetry`). Vite's emptyOutDir is
# off so this .gitignore + placeholder survive rebuilds.
index.telemetry.html
assets/
favicon.ico
sluice.png
sluice.svg
arbiter.svg
