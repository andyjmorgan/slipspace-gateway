[project]
name = "sluice-gateway-pycompat"
version = "0.0.1"
description = "Wire-compatibility suite that drives the official OpenAI / Anthropic / Google Gemini Python SDKs against sluice-gateway."
requires-python = ">=3.10"
dependencies = [
    "openai>=1.50.0",
    "anthropic>=0.40.0",
    "google-genai>=0.5.0",
    "pytest>=8.0",
    "pytest-timeout>=2.3",
    "requests>=2.32",
]

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = []

[tool.pytest.ini_options]
timeout = 60
addopts = "-v --tb=short"
testpaths = ["."]

[tool.ruff]
target-version = "py310"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]
ignore = ["E501"]
